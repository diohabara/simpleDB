load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "boolean",
    srcs = [
        "boolean_type.cc",
    ],
    copts = [
        "-std=c++17",
    ],
    deps = [
        "//src/includes/type:boolean",
    ],
)
